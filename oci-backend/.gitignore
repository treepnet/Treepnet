# Secrets and generated files — NEVER commit these.
.env
functions/fcm-service-account.json
postgrest/jwks.json

# Database dumps and runtime state.
*.dump
caddy_data/
caddy_config/

# Chat backend — separate repo (SirojiddinBoltayev/messenger-chat-backend), deployed to server
chatbackend/
